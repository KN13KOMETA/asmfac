.global _start
.intel_syntax noprefix

_start:
  # write
  mov rax, 1
  mov rdi, 1
  lea rsi, [message]
  mov rdx, 4
  syscall

  # exit
  mov rax, 60
  mov rdi, 0
  syscall

message:
  .asciz "Hi!\n"
